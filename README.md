# Douban Crawler

The project develops a Python application to scrape movie information and users' movie reviews from the Douban platform (similar to IMDb), preprocess the scraped data (removing duplicates and merging by movie), export the data to JSON files, and update the data daily.
