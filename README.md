# Douban Crawler

The project develops a Python application to scrape movie information and users' movie reviews from the Douban platform (similar to IMDB), pre-process the scraped data (removing duplicates and merging by movie), export data to JSON files, and update data periodically.
